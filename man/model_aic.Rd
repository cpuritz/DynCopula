% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aic.R
\name{model_aic}
\alias{model_aic}
\alias{model_aic.list}
\alias{model_aic.SingleCellExperiment}
\title{Model AIC}
\usage{
model_aic(res, ...)

\method{model_aic}{list}(res, cores = 1L, ...)

\method{model_aic}{SingleCellExperiment}(res, ...)
}
\arguments{
\item{res}{A \code{list} or a \code{SingleCellExperiment}.}

\item{...}{Other arguments.}

\item{cores}{Number of cores to use. Default is \code{1}. Only used when
\code{res} is a \code{list}.}
}
\value{
If \code{res} is a \code{list}, returns the model AIC. If \code{res}
is a \code{SingleCellExperiment}, returns the input
\code{SingleCellExperiment} but with the metadata entry \code{dyn_corr}
updated to include a new entry \code{aic} storing the model AIC.
}
\description{
Compute the model AIC. Requires that coefficients were computed
at all time points.
}
\details{
If \code{res} is a \code{list}, it must be the output of
\link[DynCopula]{fit_dynamic_gaussian}. In this case, correlation
coefficients must have been estimated at all time points (i.e.
\link[DynCopula]{fit_dynamic_gaussian} must have been run with
\code{x0 = x}). If \code{res} is a \code{SingleCellExperiment}, is must be
the output of either \link[DynCopula]{fit_dyn_corr} or
\link[DynCopula]{fit_dyn_corr_sel}.
}
\section{Methods (by class)}{
\itemize{
\item \code{model_aic(list)}: Method for \code{list}.

\item \code{model_aic(SingleCellExperiment)}: Method for \code{SingleCellExperiment}.

}}
