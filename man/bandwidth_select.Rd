% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_fit.R
\name{bandwidth_select}
\alias{bandwidth_select}
\title{Bandwidth selection for a dynamic Gaussian copula model}
\usage{
bandwidth_select(
  NX,
  x,
  bandwidths,
  xind = length(x),
  degree = 0L,
  control = list(),
  cores = 1L
)
}
\arguments{
\item{NX}{Matrix of normal-transformed pseudo-observations at covariate
values.}

\item{x}{Vector of covariate values corresponding to \code{NX}. Must be
sorted and have no duplicates.}

\item{bandwidths}{Vector of kernel bandwidths to test.}

\item{xind}{Number of covariate values to use for LOOCV. Default is
\code{length(x)}.}

\item{degree}{Degree of local polynomial approximation. Default is \code{0}.}

\item{control}{A \code{list} of control parameters for optimization.}

\item{cores}{Number of cores to use. Parallelized over \code{bandwidths}.
Default is \code{1}.}
}
\value{
A \code{data.frame} specifying the cross-validated likelihood
criterion at each bandwidth value.
}
\description{
Select the optimal kernel bandwidth for a dynamic Gaussian
copula model using leave-one-out cross validation (LOOCV).
}
\details{
See \link[DynCopula]{fit_dynamic_gaussian} for details on parameter
estimation.

The argument \code{xind} specifies the number of covariate values to use for
LOOCV. Full LOOCV corresponds to \code{xind = length(x)}. If
\code{xind < length(x)}, then only a subset of the covariates are used to
reduce the run time. The indices of the chosen covariates are equally spaced
along \code{seq_along(x)}. This is only an estimate to full LOOCV and may be
quite inaccurate for \code{xind << length(x)}.

The optimal bandwidth is the one that maximizes the cross-validated
likelihood criterion.
}
